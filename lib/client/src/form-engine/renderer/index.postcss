@import "@/css/mixins/scroller";

.bkform-engine-renderer {
    position: relative;
    background: #ffffff;
    overflow: auto;
    @mixin scroller;
}
.bkform-engine-renderer-fields {
    position: relative;
    padding: 24px 32px;
    overflow: auto;
    @mixin scroller;
    &.half-row-layout {
        display: flex;
        flex-wrap: wrap;
        align-content: flex-start;
        justify-content: space-between;
        .bkform-engine-field-widget {
            width: calc(50% - 5px);
            &.full-row {
                width: 100%;
            }
        }
    }
    .user-selector {
        width: 100%;
    }
}
.bkform-engine-field-widget {
    margin-bottom: 24px;
}
.bkform-engine-renderer-actions {
    padding: 12px 32px;
    button {
        margin-right: 8px;
    }
}
