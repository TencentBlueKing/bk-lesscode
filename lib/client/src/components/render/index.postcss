@import "@/css/mixins/scroller";
.render {
    height: calc(100vh - 164px);
    overflow-x: auto;
    overflow-y: hidden;
    @mixin scroller;
    :global(#render) {
        height: 100%;
        width: 100%;
    }
}
