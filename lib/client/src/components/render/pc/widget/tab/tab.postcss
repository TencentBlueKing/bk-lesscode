.render-widget-tab {
    pointer-events: all;
    .bk-tab-header {
        pointer-events: all;
        * {
            pointer-events: all;
        }
    }
    .bk-lesscode-tab-header {
        pointer-events: all;
        * {
            pointer-events: all;
        }
    }
}
